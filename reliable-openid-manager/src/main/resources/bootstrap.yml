server:
  port: 8820
eureka:
  instance:
    prefer-ip-address: true 
    ip-address: 47.104.89.2
  client:
    service-url:
      defaultZone: http://47.104.89.2:8000/eureka/
spring:
  application:
    name: reliable-openid-manager
  cloud:
    config:
      name: rabbitmq-bus,async-thread,logback-all-info,redis-3-openid
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: reliable-config